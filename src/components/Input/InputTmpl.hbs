<input id="{{id}}" name="{{name}}" type="{{type}}" placeholder="{{placeholder}}" class="{{styles.inputContainer__input}}"
           value="{{value}}" pattern="{{pattern}}"
           {{#if disabled}}
           disabled
           {{/if}}
></input>

