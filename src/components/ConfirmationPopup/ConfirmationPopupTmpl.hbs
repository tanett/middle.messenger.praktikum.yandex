<div  class="{{styles.popup__overlay}}">
  <div class="{{styles.popup__container}}">
    {{{Button
      text="X"
      classNames='popup__closeBtn'
      type="button"
      id="popupClsBtn"
      onClick=onCloseClick
      secondary="true"
    }}}
    <h1 class="{{styles.popup__title}}">{{ title }}</h1>
   <p class='{{styles.popup-confirm__message}}'>{{message}}</p>

    <div class='{{styles.popup-confirm__btn-wrapper}}'>
      {{{Button
        text="Отменить"
        classNames=""
        type="button"
        id="popupCanceleBtn"
        onClick=onCloseClick

      }}} {{{Button
      text="Подтвердить"
      classNames='button_primary'
      type="button"
      id="popupSaveBtn"
      onClick=onSaveClick

    }}}
    </div>

  </div>
</div>


